<?xml version="1.0" encoding="utf-8"?>
<!--
    Regra para Android 12 e posteriores (`android:dataExtractionRules`).

    Duas seções, e as duas excluem tudo. `cloud-backup` é o que `allowBackup`
    controla; `device-transfer` **não é** — ela cobre a cópia direta de aparelho
    para aparelho na configuração inicial, e continuaria copiando o cache mesmo
    com o backup em nuvem desligado. É por isso que as duas estão aqui: desligar
    só o backup deixaria a porta de transferência aberta.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
